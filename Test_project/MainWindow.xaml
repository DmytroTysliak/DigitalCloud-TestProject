<Window x:Class="Test_project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test_project"
        xmlns:Search_button="clr-namespace:Test_project.View.Search_button"
        xmlns:Changes_buttons="clr-namespace:Test_project.View.Changes_buttons"
        mc:Ignorable="d"
        Title="MainPage" Height="450" Width="800"
        Foreground="{DynamicResource ForegroundBrush}"
        Background="{DynamicResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <DataGrid x:Name="TopCurrenciesGrid"
        ItemsSource="{Binding Currencies}"
        SelectedItem="{Binding SelectedCurrency, Mode=TwoWay}"
        AutoGenerateColumns="False"
        HorizontalAlignment="Stretch"
        Margin="5"
        MouseDoubleClick="CurrenciesGrid_MouseDoubleClick"
        Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Currency Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="*"/>
                <DataGridTextColumn Header="Price (USD)" Binding="{Binding Price}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Search_button:Search Grid.Row ="1" Width="400" Height="60" />

        <Changes_buttons:Changes Grid.Row="2" DataContext="{Binding}" Width ="400" Height="60"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" Margin="5">
            <Button Content="Light" Width="120" Margin="5" Click="LightThemeButton_Click"/>
            <Button Content="Dark" Width="120" Margin="5" Click="DarkThemeButton_Click"/>
        </StackPanel>

    </Grid>

</Window>
